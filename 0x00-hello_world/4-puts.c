#include <stdio.h>

/**
 * main description : entry point
 *
 * Description : a program with c language that prints with put function
 *
 * return : always 0 (succes) 
*/

int main(void) 
{
puts("\"Programming is like building a multilingual puzzle");
return(0);
}
